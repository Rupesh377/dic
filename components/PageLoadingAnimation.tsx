'use client'

export function PageLoadingAnimation() {
  // Loading animation is only shown on initial page load via CSS
  // This component is intentionally minimal to avoid SSR issues
  return null
}
